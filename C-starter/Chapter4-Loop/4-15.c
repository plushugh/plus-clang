#include <stdio.h>

int main(void)
{
    int i, n;

    printf("Integer:");
    scanf("%d", &n);

    for (i=1; 1<=1;i++) {
        if (n % i == 0)
            printf("%d ", i);
    }
    putchar("\n");

    return 0;
}